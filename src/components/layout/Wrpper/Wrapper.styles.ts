import styled from "styled-components";
import theme from "../../../style/theme";

export const WrapperStyle = styled.div`
  width: 100%;
  padding: 0 25px 100px;
  ///calc(100vh + ${theme.size.gnbHeight});
  overflow-y: auto;
`;
